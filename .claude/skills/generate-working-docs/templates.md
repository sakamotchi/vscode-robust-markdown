# テンプレート詳細

このファイルには、開発作業ドキュメント生成時に使用するテンプレートを記載します。
`{要件名}` の部分はユーザーが指定した要件名に置き換えてください。

---

## requirements.md テンプレート

```markdown
# 要件定義書 - {要件名}

## 概要

この開発作業で実現したいことを記載します。

## 背景・目的

なぜこの機能が必要なのか、どのような課題を解決するのかを記載します。

## 要件一覧

### 機能要件

#### F-1: {機能名}

- **説明**:
- **受け入れ条件**:
  - [ ] 条件1
  - [ ] 条件2

#### F-2: {機能名}

- **説明**:
- **受け入れ条件**:
  - [ ] 条件1

### 非機能要件

- **パフォーマンス**:
- **エラー耐性**:
- **保守性**:

## スコープ

### 対象

-

### 対象外

-

## 実装対象ファイル（予定）

- `src/...`

## 依存関係

-

## 既知の制約

-

## 参考資料

-
```

---

## design.md テンプレート

```markdown
# 設計書 - {要件名}

## アーキテクチャ

### 対象コンポーネント

```
Extension Host (Node.js)
  └── extension.ts（activate）
        └── PreviewManager
              ├── markdownRenderer.ts
              ├── debounce.ts
              └── webview/template.ts

Webview
  └── HTML + CSS + mermaid.js
```

### 影響範囲

- **拡張機能側（Node.js）**:
- **Webview側（HTML/JS）**:

## 実装方針

### 概要

実装の全体的なアプローチを記載します。

### 詳細

1.
2.
3.

## データ構造

### 型定義（TypeScript）

```typescript
interface Example {
  uri: string;
  panel: vscode.WebviewPanel;
}
```

## VSCode API 設計

### 使用するAPI

| API | 用途 |
|-----|------|
| `vscode.window.createWebviewPanel()` | プレビューパネルの生成 |
| `vscode.workspace.onDidChangeTextDocument` | 編集イベントの検知 |

### コマンド登録

| コマンドID | 説明 |
|-----------|------|
| `robustMarkdown.{commandName}` | |

## UI設計（Webview）

### HTML構成

-

### CSP設定

```html
<meta http-equiv="Content-Security-Policy"
  content="default-src 'none'; script-src ${webview.cspSource}; style-src 'unsafe-inline';">
```

## テストコード

### ユニットテスト例

```typescript
import * as assert from 'assert';

suite('{要件名} Test Suite', () => {
  test('should do something', () => {
    assert.strictEqual(true, true);
  });
});
```

## 設計上の決定事項

| 決定事項 | 理由 | 代替案 |
|---------|------|--------|
| | | |

## 未解決事項

- [ ]
```

---

## tasklist.md テンプレート

```markdown
# タスクリスト - {要件名}

## 進捗サマリー

| 状態 | 件数 |
|------|------|
| 完了 | 0 |
| 進行中 | 0 |
| 未着手 | 0 |

## タスク一覧

### T-1: 要件定義・設計

- [ ] 要件定義書の作成
- [ ] 設計書の作成
- [ ] レビュー完了

### T-2: {タスク名}

- [ ] 実装
- [ ] テスト作成
- [ ] 動作確認（Extension Development Host）

### T-3: {タスク名}

- [ ] 実装
- [ ] テスト作成
- [ ] 動作確認（Extension Development Host）

### T-4: テスト・ドキュメント

- [ ] 自動テスト（`npm test`）
- [ ] 手動テスト（testing.md 手順）
- [ ] 永続化ドキュメント更新
- [ ] コードレビュー

## 完了条件

- [ ] 全タスクが完了
- [ ] `npm test` がパス
- [ ] `npm run lint` がエラーなし
- [ ] 永続化ドキュメントが更新済み
```

---

## task_{タスクID}.md テンプレート

このテンプレートは、タスクの詳細を記載する必要がある場合に使用します。
初期生成では作成せず、開発中に必要に応じて作成してください。

```markdown
# タスク詳細 - T-{タスクID}: {タスク名}

## 概要

このタスクで実現することを簡潔に記載します。

## 関連要件

- F-{要件ID}: {要件名}

## 実装内容

### 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `src/...` | |

### 実装詳細

#### 1. {実装項目1}

```typescript
// コード例
```

#### 2. {実装項目2}

```typescript
// コード例
```

## 技術的な検討事項

### 検討事項1

- **問題**:
- **解決策**:
- **理由**:

## テスト観点

- [ ] {テスト観点1}
- [ ] {テスト観点2}

## 進捗

| 項目 | 状態 | 備考 |
|------|------|------|
| 設計 | 完了 / 進行中 / 未着手 | |
| 実装 | 完了 / 進行中 / 未着手 | |
| テスト | 完了 / 進行中 / 未着手 | |
| レビュー | 完了 / 進行中 / 未着手 | |

## メモ・備考

-
```

---

## testing.md テンプレート

```markdown
# テスト手順書 - {要件名}

## 概要

このドキュメントでは、{要件名}機能のテスト手順を記載します。
手動テストは F5 で起動した Extension Development Host 上での操作確認が基本です。

## 前提条件

- VSCodeで本リポジトリを開いている
- `npm run compile` または `npm run watch` でビルドが完了している
- F5 を押して Extension Development Host が起動できる状態

## 手動テスト

### ケース 1: {テストケース名}

**手順:**

1. F5 を押して Extension Development Host を起動する
2. テスト用の `.md` ファイルを開く
3. {操作}を実行する

**期待結果:**

- {期待される動作}

**確認結果:**

- [ ] OK / NG

---

### ケース 2: {テストケース名}

**手順:**

1.
2.
3.

**期待結果:**

-

**確認結果:**

- [ ] OK / NG

---

## 自動テスト

### フルテスト実行

```bash
npm test
```

### リントのみ

```bash
npm run lint
```

## エッジケース

| ケース | 期待動作 | 確認結果 |
|--------|---------|---------|
| 空のMarkdownファイル | エラーなく空のプレビューを表示 | |
| 不正なMarkdown構文 | エラーをインライン表示・パネル維持 | |

## 回帰テスト

既存機能への影響がないことを確認します。

- [ ] 既存のプレビュー機能が正常に動作する
- [ ] デバウンスが正常に機能する
```
